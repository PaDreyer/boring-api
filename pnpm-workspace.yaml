packages:
  - packages/*
  - examples/*

# Install the runtime schema peer for workspace consumers.
autoInstallPeers: true

# Keep the existing TypeScript 4.9-compatible declaration dependency.
overrides:
  '@types/pg>pg-protocol': 1.7.1
